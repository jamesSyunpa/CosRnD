1. **Modify** **`build_runner.py`**: Update `collect_datas` function to explicitly include `Icon.ico` (and other potential icon files) in the build data. This ensures the icon file is available at runtime inside the frozen application.
2. **Verify**: Re-run the build to produce the corrected executable.

