I will fix the database crash by ensuring the schema migration runs reliably to add the missing columns.

**Action Plan:**

1. **Modify** **`database/db_manager.py`**:

   * Update `_check_and_run_migrations` to **always** run `_run_migrations()` regardless of the version number. This ensures that even if the version matches, any missing columns (like `manufacturing_date`) are detected and added.

   * Bump `SCHEMA_VERSION` to **21** to be safe.

**Verification:**

* The system will automatically detect the missing `manufacturing_date` column on startup and add it, resolving the `sqlite3.OperationalError`.

